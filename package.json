{
  "name": "deploy-test",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal Hello World application used as an OIDC deploy consumer",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "test": "./test/smoke.sh"
  }
}
